/*
 * Hinweis: Dieses Skript wurde beinahe vollständig von ChatGPT erstellt und dient nur zur Überprüfung der Machbarkeit
 */

using UnityEngine;
using Oculus; // optional
using UnityEngine.Rendering;

public class SendToOVRPassthrough : MonoBehaviour
{
    /*[Tooltip("Material mit Custom Shader für den Passthrough-Effekt")]
    public Material passthroughMaterial;

    private OVRPassthroughLayer passthroughLayer;

    void Start()
    {
        passthroughLayer = GetComponent<OVRPassthroughLayer>();

        if (passthroughLayer != null && passthroughMaterial != null)
        {
            passthroughLayer.overlayMaterial = passthroughMaterial;
            Debug.Log("Custom Material auf OVRPassthroughLayer angewendet.");
        }
        else
        {
            Debug.LogWarning("PassthroughLayer oder Material fehlt.");
        }
    }*/
}
